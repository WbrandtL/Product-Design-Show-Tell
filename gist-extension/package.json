{
  "name": "gist-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Floating-icon companion that turns a selected passage into a verified reading diagram, via the local gist-backend.",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json && node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.278",
    "esbuild": "^0.23.1",
    "typescript": "^5.6.2"
  }
}
