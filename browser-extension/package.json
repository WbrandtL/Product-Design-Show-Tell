{
  "name": "timeaware-browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "TimeAware companion extension: reports active-tab domain time to the local TimeAware desktop app",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json && node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.278",
    "esbuild": "^0.23.1",
    "typescript": "^5.6.2"
  }
}
